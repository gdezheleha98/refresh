module.exports = api => {
  api.registerCommand(
    "test",
    {
      description: "use mpvue loader",
      usage: "vue-cli-service test [options] [...files]"
    },
    args => {
      console.log(api.resolveChainableWebpackConfig());
      api.chainWebpack(configChain => {
        console.log(configChain.toConfig());
        return configChain;
      });
      console.log(1111);
    }
  );
  api.chainWebpack(configChain => {
    return configChain;
  });
};
