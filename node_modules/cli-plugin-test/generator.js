module.exports = api => {
  const pkg = {
    scripts: {
      mpvue: "vue-cli-service test"
    },
    eslintConfig,
    devDependencies: {
      "cli-plugin-test": "^1.0.0"
    }
  };
  api.extendPackage(pkg);
};
